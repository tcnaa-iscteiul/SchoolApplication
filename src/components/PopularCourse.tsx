import { BottomNavigation } from '@mui/material';
import Layout from './UI/Layout';

export const PopularCourse = () => {
  return (
    <Layout>
      <p>Popular Course</p>
      <BottomNavigation />
    </Layout>
  );
};
