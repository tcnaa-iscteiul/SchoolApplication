import { BottomNavigation } from '@mui/material';
import Layout from './UI/Layout';

export const MainFeature = () => {
  return (
    <Layout>
      <p>Main Feature</p>
      <BottomNavigation />
    </Layout>
  );
};
